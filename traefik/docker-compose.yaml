services:
  traefik:
    image: traefik:v3.6.5
    restart: always
    command:
      - --entrypoints.web.address=:80
    ports:
      - 80:80
    volumes:
      - /run/podman/podman.sock:/var/run/docker.sock:ro
